@model IEnumerable<App.Areas.SalaryManagement.Models.BonusSalary>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BonusLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BonusSalaryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrizeMoney)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BonusLevel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BonusSalaryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrizeMoney)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BonusSalaryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BonusSalaryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BonusSalaryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
